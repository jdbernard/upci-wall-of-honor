#set($inputRoot = $input.path('$'))
{
  "count": $inputRoot.Count,
  "ministers": [
    #foreach ($m in $input.path('$.Items[*].record.S'))
      $m #if($foreach.hasNext),#end
    #end
  ]#if($inputRoot.LastEvaluatedKey.length() > 2),
  "nextPageStartsAfter": $inputRoot.LastEvaluatedKey #end
}
